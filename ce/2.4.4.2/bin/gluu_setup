#!/bin/bash
# Author Pooya Parsa <pooya@pi0.ir>
set -e

cd /install/community-edition-setup

[ -f setup.properties.last ] && cp -v setup.properties.last setup.properties

(sleep 3 && tail -f *.log) &

yes | ./setup.py 
